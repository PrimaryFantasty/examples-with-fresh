{
  "nodeModulesDir": "auto",
  "tasks": {
    "check": "deno fmt --check . && deno lint . && deno check",
    "dev": "deno run -A --unstable-kv main.ts",
    "build": "deno run -A --unstable-kv main.ts build",
    "start": "deno serve -A ----unstable-kv main.ts"
  }
}
